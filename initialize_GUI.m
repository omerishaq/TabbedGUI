function [ ] = initialize_GUI( hTabGroup )

%%%   this code block was being used to disable the tabs in the tabbed view
%%%   but since since Matlab 2014b it has been changed since the handles to
%%%   the visual controls are no longer the doubles but actual object and
%%%   this code no longer works
%     jTabGroup = getappdata(handle(hTabGroup),'JTabbedPane');
%     
%     jTabGroup.setEnabledAt(1,false);
%     jTabGroup.setEnabledAt(2,false);
%     jTabGroup.setEnabledAt(3,false);
%     jTabGroup.setEnabledAt(4,false);
    

end

